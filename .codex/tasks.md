# Задачи

## В работе

- [ ] Довести текущий прогон `go run . --config .\configs\config.json` до загрузки расширения без ошибок
  - за последние итерации уже закрыты крупные кластеры: `Subsystem/Content`, `Role/Ext/Rights.xml`, `AdoptedStubMetaData`, `ChartOfCharacteristicTypes/Ext/Predefined`, form `DataPath` у manual-query dynamic list, удержание excluded-объектов native подсистемами и ролями
  - следующий шаг: новый чистый прогон и повторная проверка загрузки по свежему `v8_src*`
  - цель: перейти к успешной сборке `.cfe` на текущей XML-выгрузке

- [ ] Стабилизировать правила переписывания XML в `internal/utils/xmlutil/change.go`
  - цель: `go run . --config .\configs\config.json` должен давать валидный `.cfe`
  - проверка: без ошибок проверки в локальной 1С
  - риск: часть трансформаций может делать XML структурно невалидным
  - уже стабилизированы проблемные группы: cleanup служебных XML, type-bearing XML, `AdoptedStubMetaData`, form-cleanup для dynamic list
  - следующий шаг: добить оставшиеся точечные ошибки загрузки без расширения области изменений

- [ ] Удержать классификацию `Native` / `AdoptedStub`
  - источник истины: текущая семантика конфига и пользовательские правила
  - не ломать: поведение уже рабочих конфигов
  - уже зафиксированы и покрыты тестами ключевые инварианты: soft-exclude сильнее `primaryNative`, native подсистемы и native роли не восстанавливают excluded-объекты, `AdoptedStubMetaData` не переигрывает soft-exclude
  - следующий шаг: продолжать пополнять контрольные сценарии классификации по новым кейсам

## Next
- [ ] Добавить регрессионные проверки для `internal/config` и вспомогательной XML-логики
- [ ] Зафиксировать минимальные golden-сценарии для XML-конвейера
- [ ] Добавлять общие модули из макета `упо_Search` и переносить в них методы, где в исходном коде есть врезки в режиме `&После`
- [ ] Сохранять карту идентификаторов `Adopted`-объектов и уметь загружать ее при новом прогоне, чтобы можно было выпускать обновления расширения без потери mapping

## Tech debt
- [ ] Разбить монолитный XML-конвейер на более мелкие проверяемые шаги без смены поведения

## Cleanup
- [x] Убрать остаточные `run-*.log` из корня репозитория
- [x] Вести расследовательские логи только через `output/_log`

## Done
- [x] Добавлена публичная обертка `pkg/config.Load`
- [x] Добавлена публичная обертка `pkg/converter.RunConversion`
- [x] Добавлен пример запуска `cmd/app/main.go`
- [x] Активный локальный конфиг переведен на `configs/config.json`
- [x] Встроенный режим совместимости по умолчанию обновлен до `8.3.27`
- [x] Описан короткий цикл проверки и отладки в `docs/debugging.md`
- [x] Описан воспроизводимый сценарий проверки `.cfe` в локальной 1С
- [x] Синхронизирован корень расширения через `extension` / `prefix`, включая `InternalInfo/xr:PropertyState`, `Caption`, `ShortCaption`, `Language.Русский`
- [x] Выровнен корень расширения между новой выгрузкой и рабочим образцом из `Artefacts\_OldTmp`
- [x] Корневой `Configuration.xml/ChildObjects` перестроен по итоговому top-level составу, а не только по `native_prefixes`
- [x] Для `AdoptedStubExt(Form)` зафиксирован и встроен field contract dynamic list вместо эвристики только по `MainTable`
- [x] Для `Subsystem` adopted-нормализация больше не срезает верхний `ChildObjects`

## Явные пробелы
- Нет зафиксированных unit/integration tests
- Нет CI pipeline
- Нет автоматизированного сценария проверки на локальной 1С
- Assumption: текущий цикл проверки `.cfe` все еще опирается на ручной прогон в локальной 1С
